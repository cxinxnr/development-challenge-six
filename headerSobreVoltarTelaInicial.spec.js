// Generated by Selenium IDE
const { Builder, By, Key, until } = require('selenium-webdriver')
const assert = require('assert')

describe('Header-Sobre-VoltarTelaInicial', function() {
  this.timeout(30000)
  let driver
  let vars
  beforeEach(async function() {
    driver = await new Builder().forBrowser('chrome').build()
    vars = {}
  })
  afterEach(async function() {
    await driver.quit();
  })
  it('Header-Sobre-VoltarTelaInicial', async function() {
    await driver.get("https://qa.medcloud.link/")
    await driver.findElement(By.linkText("Sobre")).click()
    await driver.findElement(By.linkText("Voltar a página inicial")).click()
    assert(await driver.findElement(By.css(".sm\\3Atext-mob11xl")).getText() == "Seus exames\\\\nna nuvem")
  })
})
