// Generated by Selenium IDE
const { Builder, By, Key, until } = require('selenium-webdriver')
const assert = require('assert')

describe('Footer-TraducaoEspanhol', function() {
  this.timeout(30000)
  let driver
  let vars
  beforeEach(async function() {
    driver = await new Builder().forBrowser('chrome').build()
    vars = {}
  })
  afterEach(async function() {
    await driver.quit();
  })
  it('Footer-TraducaoEspanhol', async function() {
    await driver.get("https://qa.medcloud.link/")
    await driver.findElement(By.css(".sm\\3Amb-8")).click()
    await driver.executeScript("window.scrollBy(0, -700);")
    await driver.sleep(1000)
    assert(await driver.findElement(By.css(".sm\\3Atext-left > p:nth-child(1)")).getText() == "Deje un mensaje, pergunta o comente abajo.")
  })
})
